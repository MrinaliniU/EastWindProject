package Practice;

/*
Given a rectangular matrix m and an integer k.

Find the sum of digits within all the possible k * k square matrix inside m.

Determine which of these k * k matrix has maximum sum. There could be multiple k * k matrix that has the highest sum.
Return all the UNIQUE digits of all the k * k matrix that has maximum sum.
 */
public class BestSquares {
    int[] bestSquares(int[][] m, int k){
        int result[] = {};
        return result;
    }
}
